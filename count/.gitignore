# Local Terraform directories and files
.terraform/
.terraform.tfstate.lock.info

# Terraform state files (best practice is to use a remote backend, not local state in Git)
terraform.tfstate
terraform.tfstate.backup
*.tfstate.backup

# Terraform local lock file
.terraform.lock.hcl

# Variable files (which may contain sensitive information)
*.tfvars
*.tfvars.json
# You can include a specific non-sensitive tfvars file if desired, using a negation pattern:
# !example.tfvars

# Ignore override files as they are usually used for local overrides and not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Crash log files
crash.log

# CLI configuration files
.terraformrc
terraform.rc

# Optional: ignore graph output files generated by `terraform graph`
# *.png
# *.svg

# Optional: ignore plan output files (if you use terraform plan -out=tfplan)
# *tfplan* 